export const constValues: { [key: string]: string } = {}
export let letValues = ({} as unknown) as { [key: string]: string }
export var varValues = { initialized: true }
let unused
if (unused) {
  throw new Error('What?')
}
